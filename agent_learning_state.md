# 学习状态

- 当前主题：FastAPI + SQLAlchemy 2.0 异步数据库
- 当前阶段：Stage 1 — ORM 基础闭环
- 当前周：Week 1
- 上次完成任务：Stage 1 Day 1 — 创建异步引擎并使用 `Base.metadata.create_all()` 建表（用户已确认掌握）
- 今日分配任务：Stage 1 Day 2 — 使用 `AsyncSession` 完成单表 CRUD
- 阻碍：暂无
- 下一建议任务：Stage 1 Day 3 — 比较 `flush`、`commit`、`refresh` 与 `rollback`
- 给后续 Codex 的备注：用户使用 FastAPI、SQLAlchemy 2.0、MySQL 与 aiomysql；当前是初学阶段，每日只引入一个核心概念。完成 Day 2 前不要推进到 FastAPI 数据库依赖。
