# 2026-09-10

## 清账 · 财务对账前端（frontend/）
- 从零搭建 Vue3 + TS + Vite 单页应用，Apple 风格（#fbfbfd 浅底、毛玻璃导航、胶囊按钮、SF 字体栈）
- 页面：GlassNav / HeroSection / StepsStrip / UploadCard×2 / ProgressFlow / ResultPanel / PageFooter
- 状态机 useReconcile：idle → processing → success/error；优先 POST /api/reconcile（vite 代理 localhost:8000，字段 file_a/file_b），后端不可用时回退本地演示模式（SheetJS 动态导入，按每行第一列比对，输出四分类工作表 xlsx）
- 构建通过零类型错误；主包 89.5KB，xlsx 分包 429KB 按需加载

## 改版：纯功能界面 + 高级留白
- 按用户要求移除 GlassNav / HeroSection / StepsStrip / PageFooter（文件已删除）
- App.vue 改为居中单列纯工具页：小 logo + 标题 + 副标题 → 双上传卡片 → 开始对账 → 进度/结果/错误，大留白节奏（标题区 mb-88、上传区间距 32px/mb-64、footnote mt-80）
- 重建通过：主包 85.13KB / gzip 33.87KB

## 按 SDS 前端设计规范美化（s:/ps-save/SDS-design/规范/前端设计规范.md）
- 底图改为规范渐底：radial 高光 + linear-gradient(135deg, #f8fafc→#f3f7f4→#fbf4f0)，background-attachment: fixed
- 色彩令牌：文字 #0f172a/#56616d/#69737f，主按钮深色 #111827 实底，强调蓝 #007aff，状态绿 #34c759 / 红 #ff5f57
- 排版：letter-spacing 全部归 0，去掉 clamp 连续缩放改断点明确字号（标题 40/32px）
- 材质：结果/错误面板用 .material-panel 轻玻璃（rgb(255 255 255 / 64%) + blur 28px saturate 180%），支持 prefers-reduced-transparency 退化纯白；面板圆角统一 ≤8px；上传区改为虚线拖放边界，不套卡片
- 图标统一 lucide-vue-next（新增依赖），手写 SVG 全部移除
- 结构：页面移入 src/pages/ReconcilePage.vue，App.vue 只留应用壳
- 按钮规范：主按钮深色实底、次按钮蓝色文字无边框、active scale(0.98)、focus-visible 蓝色描边
- 构建通过：主包 89.07KB / gzip 34.83KB

## 色彩加深（用户反馈"太淡"）
- 底图加深：#eef4fb→#e8f2ec→#faeee4 + 淡蓝高光 rgb(0 122 255 / 10%)
- 主按钮由深色 #111827 改为品牌蓝 #007aff（hover #0063d1）+ 蓝色软阴影
- 上传拖放高亮加深至 rgb(0 122 255 / 8%)
