# 2026-09-11

## 后端（backend/）：uv 补齐 LangGraph Agent 开发依赖
- 原依赖：fastapi / sqlalchemy / uvicorn（pyproject 位于 backend/pyproject.toml，Python >=3.13）
- 新增依赖（uv add，已写入 pyproject.toml + uv.lock，共解析 85 包 / 新装 68 包）：
  - langgraph>=1.2.11、langchain>=1.4.0、langchain-core>=1.6.2
  - langchain-openai>=1.6.2、langchain-community>=0.4.2
  - langgraph-checkpoint-sqlite>=3.1.1（本地持久化 checkpointer，自带 aiosqlite/sqlite-vec）
  - langchain-mcp-adapters>=0.3.2（挂 MCP 工具）
  - python-dotenv>=1.2.3、pydantic-settings>=2.15.0
  - httpx>=0.28.1、aiofiles>=25.1.0、tiktoken>=0.14.0
- 验证：uv run python 逐模块 import 全部 OK（langgraph / langchain / langgraph.checkpoint.sqlite / langchain_mcp_adapters 等 12 项）
- 注意：本次 uv 安装耗时约 23 分钟（hardlink 失败回退 copy，跨盘符）。建议后续设置 `UV_LINK_MODE=copy` 或 `UV_CACHE_DIR` 到同一盘符以提速
